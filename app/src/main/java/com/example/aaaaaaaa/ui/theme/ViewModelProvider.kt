package com.example.aaaaaaaa.ui.theme

import androidx.lifecycle.ViewModelProvider.AndroidViewModelFactory
import androidx.lifecycle.viewmodel.CreationExtras
import androidx.lifecycle.viewmodel.initializer
import androidx.lifecycle.viewmodel.viewModelFactory


/*object AppViewModelProvider {
    val Factory = viewModelFactory {
        initializer {
            RegisterViewModel(App().container.usersRepository)
        }
        initializer {
            UsersViewModel(App().container.usersRepository)
        }
    }
}

fun CreationExtras.App(): Unidad3Application =
    (this[AndroidViewModelFactory.APPLICATION_KEY] as Unidad3Application)


 */